<%= form_with(model: recipe_type, local: true) do |form| %>
  <% if recipe_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipe_type.errors.count, "error") %> prohibited this recipe_type from being saved:</h2>

      <ul>
      <% recipe_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, 'Tipo da Receita' %>
    <%= form.text_field :name,  id: 'Tipo da Receita'%>
  </div>

  <div class="actions">
    <%= form.submit 'Enviar uma receita' %>
  </div>
<% end %>
